---
import { Card } from "uc-theme/components";

const {
  title,
  date,
  description,
  url = "#",
  thumbnail,
  badge = "GUIDE",
} = Astro.props;
---

<Card
  type="blog"
  title={title}
  url={url}
  image={thumbnail}
  badge={badge}
  date={date}>{description}</Card
>
