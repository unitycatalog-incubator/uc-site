---
const {
  title,
  date,
  excerpt,
  url = "#",
  thumbnail,
  badge = "GUIDE",
} = Astro.props;
---

<div class="blog-card-container">
  <a href={url || "#"}>
    {thumbnail && <img class="blog-image" src={thumbnail?.src} alt={title} />}
    <div class="blog-content">
      <h3>{title}</h3>
      <p>{excerpt}</p>
      <div class="blog-meta-row">
        <span class="blog-label">{badge}</span>
        <span class="blog-date">{date}</span>
      </div>
    </div>
    <div class="blog-gradient"></div>
  </a>

  <style>
    .blog-card-container {
      display: flex;
      flex-direction: column;
      background: var(--color-accent-1000);
      border-radius: 1rem;
      overflow: hidden;
      color: var(--color-neutral-0);
      text-decoration: none;
      transition: transform 0.15s;
      height: 100%;
      position: relative;
    }
    .blog-card-container a {
      color: inherit;
      text-decoration: none;
      display: flex;
      flex-direction: column;
      height: 100%;
    }
    .blog-image {
      border-radius: 1rem;
      object-fit: cover;
      height: 200px;
      min-height: 15rem;
      width: 100%;
    }
    .blog-content {
      padding: 1.25rem;
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
    }
    .blog-content h3 {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 0.75rem;
      line-height: 1.2;
    }
    .blog-content p {
      font-size: 1rem;
      margin-bottom: 1.5rem;
      color: var(--color-neutral-1000);
      flex: 1 1 auto;
      display: -webkit-box;
      -webkit-line-clamp: 3;
      -webkit-box-orient: vertical;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .blog-meta-row {
      display: flex;
      align-items: center;
      justify-content: flex-start;
      margin-top: auto;
    }
    .blog-label {
      font-size: 0.8rem;
      color: var(--color-neutral-0);
      background: var(--color-accent-1100);
      border-radius: 0.5rem;
      padding: 0.3rem 0.9rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-weight: 600;
      margin-right: 0.5rem;
    }
    .blog-date {
      font-size: 0.95rem;
      color: var(--color-neutral-400);
      font-weight: 500;
    }
    .blog-gradient {
      position: absolute;
      left: 0;
      right: 0;
      bottom: 0;
      height: 6px;
      border-radius: 0 0 1rem 1rem;
      background: var(--conic-gradient);
    }
  </style>
</div>
