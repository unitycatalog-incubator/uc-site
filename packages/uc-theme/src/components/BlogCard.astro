---
import Card from "./Card.astro";
const {
  title,
  date,
  excerpt,
  url = "#",
  thumbnail,
  badge = "GUIDE",
} = Astro.props;
---

<Card
  type="blog"
  title={title}
  body={excerpt}
  url={url}
  image={thumbnail}
  badge={badge}
  date={date}
/>
